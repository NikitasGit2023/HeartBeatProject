<UserControl x:Class="HeartBeatProjectInstaller.Pages.InstallPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="40,30,40,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Installing Service" FontSize="20" FontWeight="Bold"
                   Foreground="#1565C0" Margin="0,0,0,6"/>

        <TextBlock x:Name="StatusText" Grid.Row="1" Text="Preparing…" FontSize="13"
                   Foreground="#757575" Margin="0,0,0,14"/>

        <!-- Custom rounded progress bar -->
        <ProgressBar x:Name="ProgressBar" Grid.Row="2" Height="8" Minimum="0" Maximum="100"
                     Value="0" Margin="0,0,0,18" Foreground="#1565C0" Background="#E3F2FD">
            <ProgressBar.Template>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                        <Border x:Name="PART_Indicator" HorizontalAlignment="Left"
                                Background="{TemplateBinding Foreground}" CornerRadius="4"/>
                    </Border>
                </ControlTemplate>
            </ProgressBar.Template>
        </ProgressBar>

        <!-- Log output -->
        <Border Grid.Row="3" Background="#FAFAFA" BorderBrush="#E0E0E0"
                BorderThickness="1" CornerRadius="4">
            <TextBox x:Name="LogBox" IsReadOnly="True" Background="Transparent"
                     BorderThickness="0" FontFamily="Consolas" FontSize="12"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     Padding="12" AcceptsReturn="True" Foreground="#424242"/>
        </Border>
    </Grid>
</UserControl>
